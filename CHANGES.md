# Changelog (local edits)

- Add Turkish characters to keyboard layout (`src/mindaffectBCI/resources/ui_data/keyboard.txt`).
- Expand inline `_FakeUtopia` stub in `selectionMatrix.py` to implement selection/prediction handlers and signal-quality getters for offline/testing mode.
- Minor runtime test and verification performed; GUI runs with `skip_connect=True`.
